{
  "name": "myproject",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.mjs",
    "dev": "nodemon -e js,mjs,pug,css --exec node index.mjs",
    "build:docker": "docker build -t se-docker-express-pug .",
    "start:docker": "docker run -d -p 3000:3000 se-docker-express-pug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.2.0",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "description": ""
}
