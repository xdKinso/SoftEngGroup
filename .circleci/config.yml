version: 2.1

jobs:
  works-job:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - checkout
      - run: docker compose up -d --build
      - run: echo "hello world"                  # Install dependencies
      - run: node src/index.mjs

workflows:
  my-workflow:
    jobs:
      - works-job
